require "spec_helper"

describe ExceptionHandler do
  it "has a version number" do
    expect(ExceptionHandler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
